import 'package:flutter/material.dart';

class CharactersCard extends StatelessWidget {
  const CharactersCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      
    );
  }
}